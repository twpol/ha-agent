{
  "mqtt": {
    "_": "configuration for MQTT server inside Home Assistant",
    "server": "hostname or IP",
    "port?": "port number (default value: `1883`)",
    "username?": "username",
    "password?": "password"
  },
  "homeassistant?": {
    "prefix?": "MQTT topic prefix (default value: `homeassistant`)",
    "deviceName?": "Display name for this device in Home Assistant (default value: current hostname)"
  }
}
